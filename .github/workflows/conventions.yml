name: conventions

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: convention-lint
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: run convention checks
        run: bash scripts/lint/check-conventions.sh
